'use strict';

// Use for populating dummy data in MongoDB

/*
var mongoose = require('mongoose'),
Thing = mongoose.model('Thing');

//Clear old things, then add things in
Thing.find({}).remove(function() {
    Thing.create({ 
	
    }, function(err) {
	console.log('finished populating things');
    });
});
*/
